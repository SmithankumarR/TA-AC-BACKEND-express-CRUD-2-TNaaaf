<!DOCTYPE html>
<html>

<head>
  <title>article details</title>
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body class="bg-blue-300">
  <% include partials/header %>
  <section>
    <div class="max-w-screen-xl px-4 py-32 mx-auto lg:h-screen lg:items-center lg:flex">
      <div class="max-w-xl mx-auto text-center articleDetails">
        <h1 class="text-3xl font-extrabold sm:text-5xl">
          <%= article.title %>
            <strong class="font-extrabold text-red-700 sm:block">
              <% article.tags.forEach(t=> { %>
                <%= t %>
                  <% }) %>
            </strong>
        </h1>

        <p class="mt-4 sm:leading-relaxed sm:text-xl">
          <%= article.description %>
        </p>
        <div class="flex flex-wrap justify-center gap-4 mt-8">
          <a class="block w-full px-12 py-3 text-sm font-medium text-white bg-blue-600 rounded shadow sm:w-auto active:bg-red-500 hover:bg-red-700 focus:outline-none focus:ring"
            href="/articles/<%= article.id %>/like">
            Like
          </a>
          <a class="block w-full px-12 py-3 text-sm font-medium text-white bg-green-600 rounded shadow sm:w-auto active:bg-red-500 hover:bg-red-700 focus:outline-none focus:ring"
            href="/articles/<%= article.id %>/edit">
            Edit
          </a>
          <a class="block w-full px-12 py-3 text-sm font-medium text-white bg-red-600 rounded shadow sm:w-auto active:bg-red-500 hover:bg-red-700 focus:outline-none focus:ring"
            href="/articles/<%= article.id %>/delete">
            Delete
          </a>
        </div>
      </div>
    </div>
  </section>

</body>

</html>